name: One-time STLuxe v37

on:
  push:
    paths:
      - '.github/workflows/one-time-v37.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply focused v37 hero patch
        run: python .github/scripts/patch-stluxe-v37-hero-actions.py
      - name: Validate JS
        run: node --check mobile-stluxe-final-v22.js
      - name: Commit focused changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add mobile-stluxe-final-v22.js index.html
          git diff --cached --quiet && exit 1 || true
          git commit -m "Add STLuxe hero info and action buttons v37"
          git push
