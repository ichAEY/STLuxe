name: One-time STLuxe v41 about

on:
  push:
    paths:
      - '.github/workflows/one-time-v41-about.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Update about cache bust
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text()
          old='mobile-stluxe-about-v38.js?v=20260831-v40-salon-about'
          new='mobile-stluxe-about-v38.js?v=20260831-v41-one-card'
          if old not in s:
              raise SystemExit('about script anchor not found')
          p.write_text(s.replace(old,new,1))
          PY
      - name: Validate about JS
        run: node --check mobile-stluxe-about-v38.js
      - name: Commit cache bust
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git diff --cached --quiet && exit 1 || true
          git commit -m "Cache bust STLuxe about v41"
          git push
